import { Routes } from '@angular/router';
import { AppShell } from './Usuarios/components/app-shell/app-shell/app-shell';

export const routes: Routes = [
    
    {
        path:'admin',
        component:AppShell
    }
];
